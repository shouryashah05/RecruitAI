# ── RecruitAI: Headless MVP Dependencies ──────────────────────────────
# LangGraph orchestration
langgraph==0.2.62
langgraph-checkpoint==2.1.2
langgraph-checkpoint-sqlite==2.0.11
langchain-core==0.3.29
langchain-google-genai==2.0.5

# Retries for flaky external APIs (Gemini, LlamaParse, SMTP)
tenacity==9.1.4

# LLM (Gemini) + PDF parsing (LlamaParse)
google-generativeai==0.8.3
llama-parse==0.5.7

# Data validation & config
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Database (MongoDB) — both sync and async drivers available
pymongo==4.9.2
motor==3.6.0

# Email handling (stdlib: imaplib, smtplib, email) — no extra deps
# PDF I/O helpers
pypdf==5.1.0

# PDF report generation (charts + layout)
matplotlib==3.11.2
reportlab==5.0.1

# Dev utility
typing-extensions==4.12.2

# Testing
pytest==9.1.1
